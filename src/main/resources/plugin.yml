name: BedwarsPlugin # the plugins name as it should appear in the plugin list /pl
version: 1.0 # the plugin's version
author: denis0001-dev
main: ru.morozovit.plugin.bedfight.BedfightPlugin
api-version: '1.20' # the version of the API you want to use, required starting with 1.13
commands:
  requeue:
  stat: